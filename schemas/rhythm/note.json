{    
    "$schema": "https://json-schema.org/draft-07/schema#",
    "title": "Note definition",
    "description": "JSON Schema for note definitions",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "note": {
            "type": "string",
            "enum": ["dom", "rest", "tak"]
        }
    },
    "required": ["note"]
}