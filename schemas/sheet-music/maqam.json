{    
    "$schema": "https://json-schema.org/draft-07/schema#",
    "title": "Maqam definition",
    "description": "JSON Schema for Maqam definitions",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "maqam": {
            "type": "string",
            "enum": ["kurdi"]
        },
        "pitch": {
            "type": "string",
            "enum": ["a"]
        }
    },
    "required": ["maqam", "pitch"]
}