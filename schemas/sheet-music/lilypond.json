{    
    "$schema": "https://json-schema.org/draft-07/schema#",
    "title": "Lilypond music definition",
    "description": "JSON Schema for Lilypond music definitions",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "chords": {
            "type": "string"
        },
        "notes": {
            "type": "string"
        }
    },
    "required": ["notes"]
}